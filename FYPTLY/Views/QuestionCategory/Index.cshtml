@model IEnumerable<FYPTLY.Models.Question_Category>

@{
    ViewBag.Title = "Index";
}

<h2>Question Category</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.question_category_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.question_category_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Question_Category_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Question_Category_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Question_Category_id })
        </td>
    </tr>
}

</table>
